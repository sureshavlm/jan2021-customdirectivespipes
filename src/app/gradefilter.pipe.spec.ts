import { GradefilterPipe } from './gradefilter.pipe';

describe('GradefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GradefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
